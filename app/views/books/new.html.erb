<h2 class="mt-3">New book</h2>
  <%= form_with model: @book, url: '/books', method: :post, local: true do |f| %>
  # <form action="/books" method="post">
    <div class="form-group">
      <label for="book_title">Title</label>
        <input class="form-control" type="text" name="book[title]" id="book_title" />
    </div>
    <div class="form-group">
      <label for="book_body">Opinion</label>
        <textarea class="form-control" name="book[body]" id="book_body">
        </textarea>
    </div>
    <div class="form-group">
      <input type="submit" value="Create Book" class="btn btn-success" />
    </div>
  </form>